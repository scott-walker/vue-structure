// Зависимости модуля app
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default ({ invoke }: { invoke: (address: string) => unknown }) => {
  // В будущем здесь могут быть зависимости для app модуля
  // Например, сервисы для работы с API приложения

  return {
    // "@app/services/AppService": () => new AppService()
  }
}
